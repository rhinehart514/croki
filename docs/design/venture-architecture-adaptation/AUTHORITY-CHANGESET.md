# Living Venture Atlas authority changeset

> **Applied decision receipt — 2026-07-15.** The doctrine changes below have been reconciled into the
> current authority files and are preserved here to explain the transition. Do not apply this file as
> a patch or treat its imperative language as unfinished work. Current authority lives in
> [`../../FIRM-SPEC.md`](../../FIRM-SPEC.md) and [`../../STATE.md`](../../STATE.md).

## `docs/FIRM-SPEC.md`

Replace the three-noun ceiling with a two-layer rule:

> Drover's founder-facing operating kernel is venture architecture, bet, release, outcome, teammate, and the founder wall. The architecture accepts open founder-defined concepts, but only product loops, systems, motions, and campaigns have host-defined operational semantics. New nouns are justified only when they change execution, authority, attribution, or durable evidence; presentation categories and machinery remain projections.

Retain unchanged:

- founder-only ending of bets;
- non-forgeable wall authority;
- venture isolation;
- repository-bound truth;
- exact work, receipts, outcome lineage, and evidence honesty;
- provider-neutral runtimes;
- placement as presentation; and
- local readable transfer.

Delete or rewrite assertions that:

- teammate, bet, and outcome are sufficient for venture comprehension;
- all architecture must be unstructured bet content;
- the far view must project teammates and bets first;
- goals or measurement contracts are universally prohibited; or
- any additional founder-facing durable structure is necessarily machinery creep.

Add the architecture authority, mutation, evidence, transfer, deletion, and wall invariants from `LIVING-VENTURE-ATLAS-SPEC.md`.

## `PRODUCT.md`

Change the product center to:

> Drover is the living venture atlas where a founder shapes how each venture creates value and reaches the market, then directs persistent teammates through that architecture. It stays open to founder language while connecting consequential parts of the map to exact work, founder authority, and returned reality.

The primary loop becomes:

1. orient in the venture atlas;
2. select or shape architecture;
3. direct the selected context;
4. teammates fork bets and stage exact work;
5. Jacob decides consequential releases at the wall;
6. reality returns with attributable joins;
7. Jacob applies, revises, or rejects what that evidence means for the architecture;
8. the next work begins with the changed context.

Retire any claim that agent machinery or bet territory is the primary opening experience.

## `DESIGN.md`

Add the living-atlas doctrine:

- composed map at rest, open shaping on demand;
- spatial continuity and camera/focus semantic zoom;
- ordinary concepts require no type prompt;
- operational promotion exposes consequences;
- no permanent inspector or toolbox;
- meaningful regions and routes instead of uniform node cards;
- pressure is localized and reasoned, never scored;
- evidence, inference, and unattributed signals are visually distinct without color alone;
- machinery appears through execution inspection;
- wall remains the visible world boundary; and
- outline provides deterministic parity.

Remove any far-view requirement that makes teammates, avatars, or full bet cards the venture's primary landmarks.

## `docs/design/DESIGN.md`

Record the visual grammar in `LIVING-VENTURE-ATLAS-SPEC.md` as the current canvas doctrine. Preserve the existing token source, typography, warm mineral material, motion restraint, desktop stance, density rules, and accessibility requirements.

Mark prior one-canvas, GTM engine, emergent-motion, capability-density, and causal-canvas documents as evidence rather than parallel authority.

## `docs/STATE.md`

Add a dated adaptation entry separating:

- decision complete;
- standalone prototype mechanically exercised;
- production persistence unbuilt;
- production atlas projection unbuilt;
- execution coupling unbuilt;
- migration unbuilt;
- outside-founder comprehension unproven; and
- real attributable outcome changing later work unproven.

Never call the prototype or deterministic fixtures user proof.

## Anti-cage tests

Replace noun prohibition with minimality and truth guards:

- no actor, need, promise, offering, capability, surface, release, metric, task, milestone, owner, stage, maturity, score, or workflow collection;
- no system/motion/campaign-specific stores;
- no generic executable edge taxonomy;
- no stored campaign/motion health or status;
- open concepts cannot affect execution until promoted;
- agent inference cannot mutate current architecture;
- architecture cannot issue outward authority;
- placement deletion loses no semantic architecture;
- architecture module sizes remain within repository limits; and
- Buffalo fixture language never appears in universal schema or production copy.

Retain existing wall, founder-ending, venture-isolation, transfer, security, provider-neutrality, and evidence-lineage tests.

## `AGENTS.md`

Do not add a tutorial or implementation checklist. Add only the durable product language and authority pointer after the product contract lands:

> Drover opens as a living venture atlas. Founder-defined concepts remain open; product loops, systems, motions, and campaigns become operational only when they must change execution or evidence. Bets, exact releases, outcomes, the wall, and machinery remain connected behind that architecture. `docs/FIRM-SPEC.md` is authoritative.

Preserve the existing product-agnostic, desktop, terminology, engineering-size, and commit rules.

## Application order

1. Reconcile current founder-owned edits in the authority files.
2. Apply the product-center and enduring-rails changes to `FIRM-SPEC.md`.
3. Align `PRODUCT.md`, `DESIGN.md`, and `docs/design/DESIGN.md` to that authority.
4. Record built/unbuilt truth in `STATE.md`.
5. Replace anti-cage noun prohibitions with the minimality guards.
6. Add the short `AGENTS.md` pointer last.
7. Run documentation-link checks and the full deterministic suite before implementation.

If the authority files disagree after this sequence, stop implementation. A decision package must not remain a competing source of truth.
