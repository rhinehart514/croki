# Orbital Atlas elevation receipt

Date: 2026-07-15  
Surface: desktop venture picker, Orbit, same-object bet unfold, Dive, Propose  
Live verification URL: `http://127.0.0.1:4317`

## Hand held

- Living cartography, not a dashboard: the orbit remains the primary composition and interface chrome stays retractable.
- Warm mineral canvas, ink, and spruce interaction. Amber remains wall-only. The undeclared proposed-state rose was dropped in favor of neutral ghost surfaces and dashed structure.
- Serif remains scarce: venture intent and gates only. Operational copy stays sans; receipts and measurements stay mono.
- Depth stays legible: opening camera, focus camera, unfold, and Dive are distinct altitude changes rather than new panels on top of the same view.

## Decisions

- The picker now opens as an asymmetric firm-level threshold with a truthful real venture count, rather than a narrow centered settings list.
- Missing intent and motion grouping remain visible as named gaps (`Direction not named`, `No motion named`); the UI does not invent architecture to make the orbit look complete.
- The opening camera frames every real bet and the wall inside overlay-aware bounds. Bet placement uses card-aware collision avoidance; the wall reserves a stable right inset.
- A fully placed canvas bench defaults to a 54px instrument rail. Teammates and capabilities become compact anchors at architecture altitude and expand only when selected.
- A selected bet unfolds in place with causal motion and is framed inside the usable field. Escape restores the prior camera.
- Read-only/browser-host state no longer claims current wall truth: it labels the snapshot as last-known and marks wall status as not current.
- Sparse Dive is an intentional centered evidence-gap state. Dense Dive preserves exact workflow lineage, readable stage titles, gates, outcomes, and receipts.
- Validated proposal objects arrive with a 30ms stagger capped at 150ms; reduced-motion users receive the final state without staged motion.

## Reference lineage

Pixel authority remains the five mockups in `docs/design/orbital-atlas/`; intent authority remains `docs/design/orbital-atlas.md` and `docs/design/orbital-atlas-build.md`. The existing StackAI, Relevance AI, tldraw computer, and FLORA references informed the graph vocabulary and canvas depth. Motion behavior follows the repository motion spine plus Motion's documented presence, layout, and reduced-motion patterns. A fresh Mobbin pull was unavailable because the connector required authentication, so no uncited visual precedent was introduced.

## Browser proof

- Before: `output/playwright/00-picker-before.png`, `01-orbit-before.png`, `02-orbit-1600-before.png`, `03-whole-venture-before.png`
- After: `output/playwright/10-picker-after.png`, `11-orbit-after-v2.png`, `16-orbit-1280-final.png`

The after captures use the real local `drover` venture. Direct browser access is intentionally read-only, so proposal submission and wall decisions remain covered by deterministic browser fixtures rather than fabricated interactions.

## Verification

- Full repository gate: 541 brain tests and 236 UI tests, then lint and production build.
- Desktop acceptance journeys: firm, overnight return, dense scale, and wall all pass.
- Atlas journeys: empty truth, dense semantic zoom, and the complete architecture trace all pass.
- `git diff --check` passes. No commit was created.

## Independent judgment boundary

The builder does not assign IA or Flow scores. The stop-ship checks for collision, wall clipping, overlay conflict, mid-word wrapping, and undeclared hue are encoded in implementation/tests and are ready for a separate audit against the original rubrics.
