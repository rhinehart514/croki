# Authorize server-derived Project resources

Croki authorizes every collaborative action from the authenticated Person, active Project membership, server-derived Thread and resource ownership, and any required Consequence Grant. Client-provided project IDs, thread IDs, paths, working directories, GitHub identity, relay identity, and token labels are never sufficient authority. Files, terminals, assets, providers, Git actions, and subscriptions must resolve through the Project boundary before execution.
