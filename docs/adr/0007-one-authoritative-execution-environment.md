# Keep one authoritative Execution Environment

Each Project's live work runs in one authoritative Croki Execution Environment, initially controlled by its owner on a local machine or persistent team server. Every human client observes and commands that same environment; Croki will not synchronize competing peer runtimes. Managed cloud environments may later implement the same boundary without changing the Project or Thread model.
