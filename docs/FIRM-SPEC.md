# Croki product specification

This document defines Croki's durable product and build physics. `docs/STATE.md` records what the
current tree proves. `DESIGN.md` defines the intended desktop experience. Translations cannot override
this file.

## Outcome

Croki is the low-latency, cross-project agentic development environment where one founder directs
native Claude and Codex without losing exact context, provider capability, or control.

Work is the default and hot path: enter a project, resume exact context, direct the selected SDK, and
inspect or apply its work. Product / GTM is that project's ambitious coherence and consequence layer:
it keeps Product and market work causal, makes consequences reviewable, learns from reality, and
improves the next direct move. It never gates ordinary Work.

Speed and improving velocity are the moat. Safety, provenance, and founder authority make that speed
usable; they are not the headline.

## Product promise

Croki lets one founder move between projects and direct native Claude and Codex with the immediacy of
a first-party coding client, while each project's Product and GTM context compounds exact work into
faster, more coherent next moves.

The value order is:

1. move between projects, Threads, and exact work with exceptional speed;
2. preserve the selected Claude or Codex SDK's native capability and direct identity;
3. pursue and compare several exact approaches without reconstructing context;
4. keep Product and GTM consequences attached to the work that caused them;
5. let market reality change the Product and make every cycle improve the next one;
6. retain founder authority over what becomes true or crosses into the world.

## Product laws

### Product and GTM are one changing system

Product truth, promise, offer, pricing, audience, experience, capability, and business model may all
change. PLG, outbound, inbound, partnerships, founder-led sales, manual onboarding, bespoke customer
work, and unusual experiments may coexist. No single route is privileged.

Completed Product work raises its Product/GTM consequences. Returned market evidence raises the
Product or GTM consequences it supports without fabricating an interpretation.

### Work is the hot path; coherence compounds it

The project is the founder-facing address for one isolated venture scope. Opening, switching,
resuming, directing, reviewing, and applying Work never waits for Product/GTM grounding, canvas
composition, or model adoption.

Work is one complete loop: **return → direct → watch → correct → verify → ship → return**. The founder
states what should become true once; Croki carries that direction through exact implementation,
correction, source-bearing proof, consequential action, and return without asking for reconstruction.
Its deliberately small visible model is Project, Thread, and the exact Review. Review is a projection
of material requiring judgment, not another durable work container. Run and attempt identity become
prominent only when interruption, steering, isolation, or comparison makes them relevant.

At rest, Work is the Thread and its composer. Exact code, preview, terminal, tests, evidence, and
decisions open beside conversation only when they exist and are relevant, using one coherent review
workspace. A founder selection in any exact material may become a grounded correction in that same
Thread. Completion must distinguish captured proof, unresolved uncertainty, and unverified agent
claims. The exact next consequential action appears at the point of review and leaves its receipt in
the Thread. Agents, tools, search, history, attempts, and shipping deepen this loop; they do not earn
separate dashboards, modes, or permanent destinations.

Each project owns isolated venture context, credentials, repositories, worktrees, Threads, Runs,
evidence, and canonical truth; switching changes the active scope rather than blending it.

Work always speaks directly to the founder-selected Claude or Codex SDK model. Product/GTM context may
inform that SDK and completed Work may raise a provisional Product/GTM consequence, but Product / GTM
never silently becomes the Work participant. A specialized agent participates only through an exact,
founder-visible invocation and never replaces the selected SDK by implication.

Product / GTM remains a first-class, project-scoped causal model: it connects the Product, drafted and
established market plays, founder gates, outward actions, and returned evidence. Its purpose is to make
direct work compound rather than to route, approve, or delay ordinary agent development.

### Founder direction starts work

No ambient agent invents unrelated work. One broad founder direction may authorize a continuing
portfolio of safe inward work. That authority is explicit, durable, scoped to its origin, visible, and
revocable. It may survive restart and wake on attributable evidence.

The scope never authorizes unrelated work, destructive action, Product-truth promotion, capability
expansion, or an outward effect.

### Native agents remain native

The selected Claude or Codex SDK model is the agent the founder talks to. Croki supplies venture
context, durable Threads and Runs, isolated work, provisional Product models, evidence, review, and
authority. It does not insert a persona, automatic router, mandatory plan, or workflow language that
reduces the provider's native capability.

The SDK decides how to investigate, collaborate, branch, and build within the granted scope.

### Canonical truth is singular; interpretation may be plural

Each project has one venture store containing one adopted current Product/GTM model and any number of
durable provisional model branches. Agents may create and revise source-bearing provisional
alternatives. Only the founder may selectively merge them into current truth.

Current truth never changes merely because an agent proposed, inferred, rendered, or placed something.
Layout is not semantic truth.

### Facts, evidence, and interpretation remain separate

Repository facts, exact work, outward execution receipts, and returned evidence retain their source
identity. Agent interpretations stay tentative and source-bearing. Founder assertions stay attributable.
A missing link remains visible; Croki never fills it with plausible prose.

### Authority follows consequence

Safe inward work may continue under an exact founder scope. Anything crossing into the world waits for
the founder at the exact point of action. The founder alone may:

- promote Product/GTM truth;
- widen or revoke continuing authority;
- grant a capability;
- execute or authorize an outward action;
- perform destructive work;
- end founder-owned active work.

An agent may prepare exact outward material and expected return. Preparation never equals execution.

### Doing things that do not scale is first-class

Manual onboarding, founder interviews, bespoke customer work, hand-built outreach, and other
unrepeatable acts are valid work. Repetition may eventually earn a reusable mechanism. Croki never
requires repeatability before useful work can exist.

### Vocabulary is earned

- **Live work** is a derived view of everything working, waiting, returned, or needing judgment.
- **Motion** is a broad route to value or growth such as PLG, outbound, inbound, or partnerships.
- **Branch** is a causal alternative or provisional Product/GTM model.
- **Mechanism** is reusable capacity earned through repetition.
- **Campaign** is an optional bounded market activation.
- **Pipeline** is a literal throughput pipeline with known stages.
- **Release** is a Product release.
- **Outward action** is any exact act that crosses into the world.
- **Evidence** is what reality returned.
- **Thread** is durable founder conversation and direction.
- **Run** is one exact agent attempt.

There is no universal Mission, Front, Initiative, Bet, Program, Pipeline, or other work container.
Historical identifiers may remain inside compatibility adapters only.

## Canonical venture model

The schema-v3 canonical model contains:

- open semantic objects and relationships;
- Threads and Runs;
- durable provisional `ModelBranch` records;
- source-bearing `ModelChange` overlays;
- founder-only `ModelMergeReceipt` records;
- direction-scoped `WorkScope` authority;
- generic `OutwardActionReceipt` records;
- work, artifacts, evidence, interpretations, and founder decisions by exact reference.

Object type is an open string. Territory is Product, GTM, both, or unset. Motion, campaign, mechanism,
audience, offer, capability, experience, feature, release, and evidence are earned facets, not a closed role enum.
A campaign does not require a primary motion. A motion does not require a repeatability claim.

### Provisional Product models

A branch overlays current truth; it does not copy the venture. Each change names its target, operation,
base digest, rationale, sources, proposer, and supersession. Current changes beneath the branch create a
visible conflict. Independent writes retry after unrelated store contention. Conflicting writes to the
same record require founder resolution.

Product/GTM conversation may project the local branch above its Work composer as a correctable visual view.
The projection may contain current truth, proposals, alternatives, evidence, unknowns, actions, gates, and
outcomes. It must not impose workflow order or causal edges that the underlying material has not earned. A
workflow is one possible projection of established mechanics, not the default artifact or adoption unit.
Corrections revise the same branch and projection; founder review remains selective over exact `ModelChange`
records.

Merge is transactional, selective, attributable, and founder-only. Merge does not close continuing
work. Closing a branch preserves its history.

### Work scopes and execution

A `WorkScope` carries its originating Thread and founder message, objective, subjects, Product-model
branches, allowed inward effects, evidence wake conditions, restart behavior, spend policy, and stop
conditions.

Provider execution uses one isolated session per Run and one isolated worktree per coding attempt.
Locks belong to exact resume identities, not to a participant. Spend reservations are atomic. Actual
concurrency follows the machine, provider, and budget; Croki exposes backpressure honestly and sets no
product-level count.

### Outward actions and evidence return

Product release, deploy, outbound message, publication, founder interview, demo, manual onboarding,
bespoke deliverable, partner action, and spend are distinct kinds of outward action. A founder interview
does not become a release.

Each action retains its Product/GTM context, prepared material, founder decision, executor receipt,
expected return, observation authority, returned evidence, and subsequent Product/GTM consequence.
Evidence returns to exact affected claims and work; similarity alone does not create a causal join.

## Founder experience

Croki ships as an Electron desktop ADE with one default operating surface and one project-scoped
coherence surface over the same canonical model.

### Work

Work is the default, full direct Claude/Codex development environment. The founder enters a project
here unless returning to an exact Product/GTM selection. Conversation holds intent and progress;
exact code, artifacts, comparisons, terminal, preview, tests, evidence, and decisions open beside it.
One direction may form several child Threads and Runs. The founder can compare, steer, stop, or continue
exact attempts. Deleting a non-root chat removes it from Work, stops its live Runs, and revokes continuing
work rooted there; exact Product truth, artifacts, and receipts remain because deleting conversation cannot
rewrite venture history. Completed implementation raises a provisional Product-model consequence.

The selected Claude or Codex SDK model remains the participant until the founder explicitly changes
it. Product/GTM material may be referenced beside the transcript or included as context; it does not
switch the participant, hide coding controls, or insert another decision layer. Any specialist is
named at the point of inclusion and participates beside rather than implicitly replacing the direct
SDK conversation.

### Product / GTM

Product / GTM is the project's coherence and consequence layer and the founder's intent map. Tools
everywhere else show state — code, dashboards, metrics — and hide what the founder is trying to do.
This canvas exists so the founder can see the product laid out, say what should be different, and have
that direction reach the agents exactly. Seeing the product is what makes directing it possible: the
map improves the prompt.

It may be entered directly and remains fully operable as an ambitious causal surface, but it is not
a setup gate, task router, or required prelude to Work. It never changes the active Work participant
without an explicit founder action.

The Product territory is the product itself, mapped as the pages a user walks through — landing,
signup, workspace, invite, share, settings, and whatever the real product contains. The map is a
proper map, left to right in walk order: entry pages at the left, the walk deepening rightward, with
page-to-page connections drawn only from links and redirects the code proves. Agents derive the
map from the actual code when a repository connects and keep it true as the code changes; the founder
corrects names, grouping, and visibility. A page the code cannot support is never fabricated, and
neither is a connection between pages.

Clicking a page is the refine moment. The node expands in place to what the page is now — an honest
summary or preview — plus prior founder direction and the thinking attached to it, above a plain
brain-dump input. Sending that input mints an exact Work Thread scoped to that piece. This
click-to-refine consequence is what keeps the map from being a static diagram; a product map without
it is a defect. Refinement is not hard-scoped to one entry point: the founder may direct from the Work
composer or from any node, and decides what matters for expressing intent.

A page earns a GTM action only after it carries an exact Product consequence. The founder first says
what should be different and for whom; the selected Claude or Codex SDK develops that direction in
the page's exact Work Thread. Once the founder adopts the consequence, **Connect this change to
market** may join an existing play or propose a drafted play for founder adoption. Croki never invents
a market workflow merely because a page exists.

At rest the map is clean: page names only, no badges. Direction, live work, and returned evidence live
one click deep on the page they belong to. Assumptions, proposals, decisions, and evidence attach to
the page they are about; whole-product thinking attaches to the product itself. Free-floating strategy
bubbles do not share the Product territory with the map.

The canvas remains one shared causal model with natural alternatives, projected around the founder's
current question rather than fitted into one exhaustive overview. It opens on the code-proven Product
walk. Selecting an adopted consequence, play, or evidence return recomposes that same canvas into a
focused consequence trace, full play, or evidence-return path; these are contextual projections, never
founder-facing modes or parallel truth. Product, shared Product/GTM truth, and GTM remain legible as
territories inside the graph rather than becoming separate canvases or fixed lanes. Curved edges
preserve reading direction across territory changes. A restrained spectrum appears only on a
relationship that crosses territory. Current truth and provisional alternatives cannot visually
collapse into one.

Automatic composition owns placement. Existing saved placement remains readable compatibility data
but does not control the current canvas, and the founder is never required to drag or organize nodes.
Agents, tools, and sources may be dropped only where Croki can resolve an exact subject and consequence;
unsupported drops refuse visibly and persist nothing. Selecting a node expands its exact detail, work,
evidence, or founder decision in place instead of opening a detached side inspector.

The GTM territory is workflows, in two honest registers. A **drafted play** is an intended workflow —
sophisticated and ambitious, never a thin sketch — visibly marked as not yet real. An **established play**
earned its standing by actually running. A draft is never displayed as established; the register marking
replaces the old rule that unproven motions stay vague blobs, because invisible intent is the exact
clouding this canvas exists to remove.

Plays are authored talk-first: the founder brain-dumps the play in conversation (later, also from a
page's GTM action), an agent drafts the full sequence onto the canvas aware of what else is already
running, and then walks the founder through it step by step. Corrections land on the exact step; any
step can branch or be refined later without redrafting the play.

A long play renders at full length, always — thirty steps across weeks lay out left to right and the
founder scrolls. The length is information: the founder should feel how heavy a play is. Steps are not
compressed into summary chapters.

A running play keeps the same drawn shape and shows derived counts on its steps — "5 waiting on reply",
"2 need your approval" — with the actual people one click behind each count. Counts derive from real
state, receipts, and returns; they are never hand-maintained. Founder gates, outward actions, and
evidence return unfold on this same canvas. Product remains connected as a dependency and consequence
rather than being forced into operational stages.

Conversation is compact and closable here, but it is never a separate conversation identity. Every
Product/GTM turn belongs to an exact normal Work Thread and uses the founder-selected Claude or Codex
SDK. Product interpretation, play drafting, journey mapping, and consequence correction remain beside
the canvas; native coding material follows that same Thread into Work. Closing conversation preserves
its draft, Thread identity, selected subject, and last coherent content.

Observed customer movement is a summoned, source-preserving overlay on the Product walk. It may show
aggregate page counts, transitions, and drop-offs for one disclosed source and time window without
reordering the map, creating pages, exposing people, or turning observed transitions into code-proven
links. Raw event rows and identifiers never enter canonical venture truth. The factual aggregate may
refresh automatically; any explanation of why it happened remains a provisional interpretation until
the founder adopts it.

#### Intent-layer decisions (founder interview, 2026-07-22)

| # | Decision | Note |
| --- | --- | --- |
| 1 | The canvas is the intent layer: the product mapped so the founder knows what to direct | Founder: "product strategy and mapping a product and code allows us to know what to prompt better if we can see it out" |
| 2 | Product map supersedes the prior no-surfaces stance | Explicit doctrine reversal; the click-to-refine consequence is what distinguishes the map from the rejected repo mirror |
| 3 | Map pieces are pages the user walks through | Not capabilities, not code structure; machinery-beneath-pages deferred |
| 4 | Agents read the code and lay out the map; founder corrects | Map must stay true to the repository with no founder upkeep |
| 5 | Click a page → see what it is now → brain-dump → exact Work Thread | Founder: refine "should not be hardscoped… founder should decide what's important to understand intent more — brain dump" |
| 6 | Every page carries one GTM action: join an existing play or agent proposes one | Proposal lands on GTM territory for founder adoption |
| 7 | At rest the map is clean — names only | Direction, work, and evidence are one click deep by founder choice |
| 8 | Strategy objects attach to their page; no free-floating bubbles in Product territory | Whole-product thinking attaches to the product itself |
| 9 | This pass scopes the Product territory only | Superseded same day by the GTM pass below (11–16) |
| 10 | V1 finish line: map a real repo's pages, click, honest summary, brain-dump, Thread starts | GTM button and evidence marks follow after this proves |
| 11 | GTM territory is workflows in two registers: drafted and established | Register marking replaces the vague-motion blob for idea-stage plays; a draft is never shown as established |
| 12 | Drafted plays are sophisticated and ambitious, drafted aware of what else is running | Founder: "we're going to make it sophisticated, ambitious plays for this GTM system… we also consider what else is running" |
| 13 | Authoring is talk-first: agent drafts the full sequence, then walks the founder through it step by step | Founder: "we go through each step together because stuff can be branched off / refined in the future"; corrections land per step |
| 14 | Long plays render at full length, always | Founder chose length-as-information over collapsible chapters |
| 15 | Running plays show derived counts on steps; people one click behind | Counts derive from real state, never hand-maintained |
| 16 | GTM build follows after the product-map v1 proves | Same canvas code; sequential, not parallel |
| 17 | Product walk is the resting projection | The founder sees the code-proven user walk before strategy machinery |
| 18 | Consequence, play, and evidence paths are contextual projections | One canonical model; no fit-all overview and no new modes |
| 19 | Product/GTM shares the T3-shaped shell and exact Work Threads | Same project, SDK participant, density, continuity, and interaction grammar; spatial center remains distinct |
| 20 | Product consequence precedes a GTM connection | Market work exists because of an intended customer effect, not because every page needs a play |
| 21 | Automatic composition replaces founder placement | Coherence is the default; saved placement is compatibility only |
| 22 | A long play opens on its owner and first three steps at readable scale | Every step remains rendered and reachable through the minimap and exact step navigation |
| 23 | Observed journey data is a summoned aggregate overlay | Repository topology remains canonical; facts and interpretation stay separate |
| 24 | Journey imports store aggregates and bounded receipts, never raw user-event payloads | Provider-neutral local JSON/JSONL/CSV first; adapters may follow without changing canvas truth |

Still unresolved, with owners: the machinery-beneath-pages layer (deferred until pages prove); how
pageless product machinery appears (deferred); direct analytics-provider adapters (deferred until the
provider-neutral receipt contract proves); and individual-level journey inspection (explicitly outside
the first release).

### First run and return

First run is one consequential choice: the founder chooses a real Product codebase and Croki derives its
name, grounds it locally, and opens Work immediately. The ordinary Work composer then asks **What are you
trying to make true?** and exposes the connected Claude or Codex runtime without making either another setup
gate. No organization, workflow, ontology, pipeline, campaign, or blank plan is configured first.

On launch, Croki restores the last active project's exact destination. Project switching is available
from either surface and returns to that project's last coherent Work Thread or exact Product/GTM
selection; it does not rebuild context from a summary. A Work restore includes the active attempt,
reviewed material, draft, scroll, and terminal or preview state when still valid. When an exact item is
unavailable, Croki preserves its durable address, explains the break, and keeps the nearest coherent
context visible.

On return, the existing Threads, paths, deltas, and gates answer what changed, what is moving, what
failed or went stale, what reality returned, what needs founder judgment, and which Product/GTM model
changed. There is no summary dashboard competing with the work.

### Measurable experience invariants

These are intended desktop acceptance targets, not claims about the current tree. They are measured
at p95 on supported dogfood hardware, with UI, local persistence, provider startup, and provider
generation reported separately:

- keyboard focus, local selection, and composer actions show visible acknowledgement within `100ms`;
- a warm project or Thread switch paints the destination identity, its last-coherent transcript or
  canvas, and the primary action within `250ms`; asynchronous refresh may continue;
- sending makes the founder turn visible by the next paint and assigns its durable Thread/Run address
  within `500ms`, before provider completion;
- provider startup and first factual activity are exposed as soon as emitted, without fake progress
  standing in for latency;
- switching, refresh, partial grounding, offline state, and failed resume never replace coherent
  content with an empty shell;
- Product/GTM grounding and rendering consume no blocking dependency on project entry, Work restore,
  provider dispatch, or direct Work controls.

If a target cannot be met, the experience preserves input, exact address, and last coherent content,
then names the delayed or failed layer. A spinner without that continuity is not recovery.

## Visual and interaction physics

Croki feels like calm velocity: near-black, precise, dense, responsive, and alive only where causality
is active. DM Sans carries operating language; mono is reserved for machine material. Blue means direct
action and focus. Amber means founder authority. A restrained spectrum identifies a Product/GTM territory
crossing. Current truth is solid; provisional truth is explicitly textured; evidence uses
source-bearing return curves.

Motion shows causality. Live work moves briefly. A founder gate becomes still. Returned evidence travels
back to the exact affected Product delta. Croki's monochrome identity mark may carry one spectrum sweep at
launch or when the system moves from idle to active; it settles immediately and never replaces factual progress.
Reduced motion preserves the same meaning without transforms.

Nodes are compact, readable pills. Exact deltas, evidence, and decisions receive the larger review
surface. Generic cards, raw object counts, status-dot theater, glowing AI decoration, raw transport
errors, and tiny metadata are product defects.

## Compatibility and deletion

Historical `bet`, `fork`, `gtm-ide`, `channel` storage records, architecture adapters, releases, and
`~/.gtm-ide` may remain readable until migration proves their removal safe. They do not define current
product language or receive new writes.

The following current product shapes are forbidden:

- Signal → Pipeline → Campaign → Outcome as a universal loop;
- fixed pipeline lanes;
- release as the universal unit of market movement;
- Heat as the founder model of autonomy;
- founder-facing System, Architecture, Firm, Bet, Fork, Teammate, or Pipeline compatibility language;
- separate Releases or System workspaces;
- organization-board, Atlas, pipeline-builder, and release-manager visual grammars.

Compatibility cannot justify contradictory current UI.

## Verification

Mechanical readiness requires Brain tests, UI unit tests, lint, production builds, dead-code,
architecture, size, bundle, site, browser, Electron, package, and acceptance gates.

Product readiness additionally requires one real dogfood loop:

```text
connect repository
→ direct the selected Claude or Codex SDK in Work
→ switch projects and resume the exact Thread without reconstruction
→ complete and review exact work
→ provisional Product model changes
→ exact outward action waits for founder
→ reality returns
→ Product delta updates
→ founder selectively merges
→ next work begins faster
```

The Work portion passes only when an empty Thread reserves no workbench; selecting exact preview,
diff, terminal, test, or activity material can ground a correction in the same composer; review keeps
the original direction, captured proof, unresolved uncertainty, and exact next consequence together;
and contextual capabilities recede when nothing remains to judge.

Tests cannot establish product coherence by themselves. The shipped first run, Work, Product/GTM,
return experience, public promise, and compatibility deletion must tell the same truth.
