{
  "name": "gtm-ide",
  "private": true,
  "version": "0.3.3",
  "description": "Drover — the operating system for one-person holding companies.",
  "author": "Drover",
  "main": "electron/main.cjs",
  "scripts": {
    "postinstall": "npm --prefix brain install && npm --prefix ui install",
    "build": "npm --prefix ui run build",
    "lint": "npm --prefix ui run lint",
    "test": "npm --prefix brain test && npm --prefix ui run test:unit && npm run lint && npm run build",
    "test:firm:browser": "npm --prefix ui run build -- --configLoader runner && node --test test/browser/firm-journey.mjs",
    "test:acceptance": "npm test && npm run test:firm:browser",
    "start": "npm run build && node brain/src/server.mjs",
    "mcp": "node brain/src/mcp.mjs",
    "build:desktop": "npm run build",
    "app": "npm run build:desktop && electron .",
    "app:dist": "npm run build:desktop && electron-builder --mac"
  },
  "devDependencies": {
    "electron": "42.5.1",
    "electron-builder": "26.15.3",
    "ws": "8.21.0"
  }
}
