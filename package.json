{
  "name": "gtm-ide",
  "private": true,
  "version": "0.3.3",
  "description": "Drover — the go-to-market desk for founders running more than one product.",
  "author": "Drover",
  "main": "electron/main.cjs",
  "scripts": {
    "postinstall": "npm --prefix brain install && npm --prefix ui install",
    "build": "npm --prefix ui run build",
    "lint": "npm --prefix ui run lint",
    "test": "npm --prefix brain test && npm --prefix ui run test:unit && npm run lint && npm run build",
    "test:terrain:browser": "npm --prefix ui run build -- --configLoader runner && node --test test/browser/terrain-journey.mjs",
    "start": "npm run build && node brain/src/server.mjs",
    "mcp": "node brain/src/mcp.mjs",
    "app:rebuild": "electron-rebuild --module-dir brain --only better-sqlite3,node-pty && codesign -s - -f brain/node_modules/better-sqlite3/build/Release/better_sqlite3.node brain/node_modules/node-pty/build/Release/pty.node",
    "build:desktop": "VITE_CONVEX_URL= npm run build",
    "app:reset-native": "npm --prefix brain rebuild better-sqlite3 node-pty",
    "app": "npm run build:desktop && electron .",
    "app:dist": "npm run build:desktop && npm run app:rebuild && electron-builder --mac && npm run app:reset-native"
  },
  "dependencies": {
    "convex": "1.42.0"
  },
  "devDependencies": {
    "@electron/rebuild": "4.0.6",
    "electron": "42.5.1",
    "electron-builder": "26.15.3"
  }
}
