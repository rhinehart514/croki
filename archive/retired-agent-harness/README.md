# Retired agent harness

This directory preserves the repository instruction and runtime artifacts retired during the August 8, 2026 migration to Jacob's global agent harness. The material is recoverable history only and is not an active source of agent, product, design, or project direction.

Retired material includes:

- the pre-migration root `AGENTS.md` and `DESIGN.md`;
- the `operate-project` instruction snippet and project consequence document;
- the project-local operate runtime event store and lock files.

The old layer duplicated global operating doctrine, prescribed a product-work procedure, and depended on a retired whole-project runtime. Durable Croki product, architecture, safety, release, testing, and design truth was rewritten into the active root files. Nothing in this directory belongs in an agent, skill, prompt, or harness discovery path.
